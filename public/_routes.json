{
  "version": 1,
  "description": "Force /api/* and /admin/* to Pages Functions. /api/* prevents GET fallthrough to static 404 before the dispatcher. /admin/* routes admin HTML through functions/admin/_middleware.ts so the code-based admin login gate runs (Sam 2026-06-11). Static sub-assets under /_next/* are unaffected.",
  "include": [
    "/api/*",
    "/admin",
    "/admin/*"
  ],
  "exclude": []
}
